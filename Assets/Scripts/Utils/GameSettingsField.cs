namespace Jerre.Utils
{
    public enum GameSettingsField
    {
        SPEED, 
        BOOST_SPEED,
        BOOST_DURATION,
        BOOST_PAUSE,
        HEALTH, 
        FIRE_RATE, 
        FIRE_DAMAGE,
        BOMB_DAMAGE,
        BOMB_MAX_LIFETIME,
        BOMB_PAUSE_TIME, 
        BOMB_EXPLOSION_RADIUS,
        POINTS_TO_WIN
    }
}
