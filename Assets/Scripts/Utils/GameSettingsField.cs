namespace Jerre.Utils
{
    public enum GameSettingsField
    {
        SPEED, 
        MAX_ACCELERATION,
        BOOST_SPEED,
        BOOST_DURATION,
        BOOST_PAUSE,
        HEALTH, 
        FIRE_RATE, 
        FIRE_DAMAGE,
        BOMB_DAMAGE,
        BOMB_MAX_LIFETIME,
        BOMB_PAUSE_TIME, 
        BOMB_EXPLOSION_RADIUS,
        BOMB_EXPLOSION_ACCELERATION,
        POINTS_TO_WIN
    }
}
